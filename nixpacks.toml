# Nixpacks configuration for Dokploy deployment
# Sirve archivos estáticos desde dist/ en puerto 80

providers = ["node"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[staticAssets]
path = "dist"
